---
title: useClaimToken
displayed_sidebar: react
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import CodeBlock from "@theme/CodeBlock";

Hook for claiming a token from a smart contract.

Available to use on smart contracts that implement the [ERC20](/contractkit/interfaces/erc20)
or [ERC721](/contractkit/interfaces/erc721) standard.

```jsx
import { useClaimToken } from "@thirdweb-dev/react";
```

## Usage

Provide your token contract as the argument.

```jsx
import { useClaimToken, useContract, Web3Button } from "@thirdweb-dev/react";

// Your smart contract address
const contractAddress = "{{contract_address}}";

function App() {
  const { contract } = useContract(contractAddress);
  const { mutateAsync: claimToken, isLoading, error } = useClaimToken(contract);

  if (error) {
    console.error("failed to claim token", error);
  }

  return (
    <Web3Button
      contractAddress={contractAddress}
      action={() =>
        claimToken({
          to: "{{wallet_address}}", // Use useAddress hook to get current wallet address
          amount: 100, // Amount of token to claim
        })
      }
    >
      Claim Token
    </Web3Button>
  );
}
```

## Parameters

### to

Likely, you will want to claim the token to the currently connected wallet address.

You can use the [`useAddress`](/react/hooks/useAddress) hook to get this value.

```jsx
import {
  useClaimToken,
  useContract,
  Web3Button,
  useAddress,
} from "@thirdweb-dev/react";

// Your smart contract address
const contractAddress = "{{contract_address}}";

function App() {
  // highlight-start
  const address = useAddress();
  // highlight-end
  const { contract } = useContract(contractAddress);
  const { mutateAsync: claimToken, isLoading, error } = useClaimToken(contract);

  if (error) {
    console.error("failed to claim token", error);
  }

  return (
    <Web3Button
      contractAddress={contractAddress}
      action={() =>
        claimToken({
          // highlight-start
          to: address, // Use useAddress hook to get current wallet address
          // highlight-end
          amount: 100, // Amount of token to claim
        })
      }
    >
      Claim Token
    </Web3Button>
  );
}
```

### amount

The amount of token to be claimed.

```jsx
import { useClaimToken, useContract, Web3Button } from "@thirdweb-dev/react";

// Your smart contract address
const contractAddress = "{{contract_address}}";

function App() {
  const { contract } = useContract(contractAddress);
  const { mutateAsync: claimToken, isLoading, error } = useClaimToken(contract);

  if (error) {
    console.error("failed to claim token", error);
  }

  return (
    <Web3Button
      contractAddress={contractAddress}
      action={() =>
        claimToken({
          to: "{{wallet_address}}", // Use useAddress hook to get current wallet address
          // highlight-start
          amount: 100, // Amount of token to claim
          // highlight-end
        })
      }
    >
      Claim Token
    </Web3Button>
  );
}
```