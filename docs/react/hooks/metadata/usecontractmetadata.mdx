---
title: useContractMetadata
slug: /react.usecontractmetadata
displayed_sidebar: react
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import CodeBlock from "@theme/CodeBlock";

Hook for reading the metadata of a smart contract.

Available to use on contracts that implement the [ContractMetadata](/contractkit/extension-contracts/contractmetadata)
interface.

```jsx
import { useContractMetadata } from "@thirdweb-dev/react";
```

## Usage

Provide a contract instance as the argument.

```jsx
import { useContract, useContractMetadata } from "@thirdweb-dev/react";

// Your smart contract address
const contractAddress = "{{contract_address}}";

function App() {
  const { contract } = useContract(contractAddress);
  const { data: metadata } = useContractMetadata(contract);
}
```

## Return Value

The hook's `data` property, once loaded, contains the following properties:

```ts
name?: string;
description?: string;
symbol?: string;
image?: string;
```
