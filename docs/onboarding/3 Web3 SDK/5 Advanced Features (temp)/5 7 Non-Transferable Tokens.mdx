---
slug: /sdk/advanced-features/non-transferable-tokens
---

import ThirdwebCodeSnippet from "../../../../src/components/ThirdwebCodeSnippet";
import QuickstartCard from "@components/QuickstartCard";

# Non-Transferable Tokens

Configure tokens in your smart contract to be non-transferable (or "soulbound").

<div className="row" style={{ marginBottom: 24 }}>
  <div className="col col--12" style={{ padding: 8 }}>
    <QuickstartCard
      name="Guide: Make your NFT or Tokens Non-Transferable"
      link="https://blog.thirdweb.com/guides/make-your-nft-non-transferable"
      image="/assets/icons/education.png"
    />
  </div>
</div>

To make tokens in your contract non-transferable, set the [transfer role](/sdk/advanced-features/permission-controls) to be empty.

```javascript
const rolesAndMembers = await contract.roles.getAll();

const updatedRoles = {
  ...rolesAndMembers,
  transfer: [],
};

await contract.roles.setAll(updatedRoles);
```
