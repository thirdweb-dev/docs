---
slug: /sdk/interacting-with-contracts/erc721signaturemint
title: Signature-Based Minting
---

import FeatureCodeSnippets from "../../../../../src/components/FeatureCodeSnippets";
import CodeSnippetWithSummary from "../../../../../src/components/CodeSnippetWithSummary";
import QuickstartCard from "../../../../../src/components/QuickstartCard";

# ERC721 Signature-Based Minting

Use [signature-based minting](/sdk/advanced-features/on-demand-minting) of NFTs in your smart contract.

You can utilize these features of the SDK on your contract if it implements [ERC721SignatureMint](/extensions/erc-721/erc721signaturemint).

<div className="row" style={{ marginBottom: 24 }}>
  <div className="col col--12" style={{ marginTop: 8 }}>
    <QuickstartCard
      name="Learn how to implement ERC721SignatureMint in your smart contract"
      link={"/extensions/erc-721/erc721signaturemint"}
      image="/assets/icons/general.png"
    />
  </div>
</div>

## Generate a signature

Generate a signature that can be utilized to mint an NFT by another wallet.

The signature specifies any information about the NFT such as the metadata and price.

:::info Admin Operation

The generation of signatures requires the wallet to have the `MINTER` role on the contract by default.

:::

<CodeSnippetWithSummary
  contract={`ERC721SignatureMint`}
  name={`signature`}
  isFeatureSnippet={true}
  isGetContractCode={false}
  showHr={true}
  dontShowSummary
/>

## Use a signature to mint an NFT

Mint an NFT using a previously generated signature.

<CodeSnippetWithSummary
  contract={`ERC721SignatureMint`}
  name={`signature`}
  isFeatureSnippet={true}
  isGetContractCode={false}
  showHr={false}
  dontShowSummary
/>
