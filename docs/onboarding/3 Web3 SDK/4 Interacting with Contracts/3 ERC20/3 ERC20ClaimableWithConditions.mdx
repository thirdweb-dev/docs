---
slug: /sdk/interacting-with-contracts/erc721/erc20drop
title: Drop
---

import FeatureCodeSnippets from "../../../../../src/components/FeatureCodeSnippets";
import CodeSnippetWithSummary from "../../../../../src/components/CodeSnippetWithSummary";
import QuickstartCard from "../../../../../src/components/QuickstartCard";

# ERC20 - Claim Conditions

You can utilize these features of the SDK on your contract if it implements [ERC20Drop](/extensions/erc20claimablewithconditions).

<div className="row" style={{ marginBottom: 24 }}>
  <div className="col col--12" style={{ marginTop: 8 }}>
    <QuickstartCard
      name="Learn how to implement ERC20Drop in your smart contract"
      link={"/extensions/erc20claimablewithconditions"}
      image="/assets/icons/general.png"
    />
  </div>
</div>

## Claim Tokens

Mint tokens to a specified address.

<CodeSnippetWithSummary
  contract={`ERC20ClaimableWithConditions`}
  name={`claimTo`}
  isFeatureSnippet={true}
  isGetContractCode={false}
  showHr={false}
  dontShowSummary
/>

## Configure claim conditions

Define the criteria of how NFTs can be claimed from your drop.

Each claim phase can define:

- When the claim phase will start
- How many tokens to drop
- How much to charge per NFT
- What currency to use to sell the tokens
- Which wallet addresses are allowed to mint (allowlist)
- How many tokens can be claimed per transaction
- How many seconds do wallets have to wait between claims

<CodeSnippetWithSummary
  contract={`ERC20ClaimableWithConditions`}
  name={`claimConditions`}
  isFeatureSnippet={true}
  isGetContractCode={false}
  showHr={false}
  dontShowSummary
/>
