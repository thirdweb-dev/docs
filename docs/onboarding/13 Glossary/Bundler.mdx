---
slug: /glossary/bundler
title: Bundler
---

A bundler is a smart contract that bundles multiple `'UserOperations'`
into a single bundle, a `handleOps` call, to pass to the `EntryPoint` contract.
It allows you to send transactions with smart wallet accounts.
