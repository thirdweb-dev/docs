---
slug: /solidity/extensions/multicall
title: Multicall
---

import ViewContractCodeButton from "@components/ViewContractCodeButton";
import ThirdwebFeatureCodeSnippet from "@components/ThirdwebFeatureCodeSnippet";

# Multicall

```solidity
import "@thirdweb-dev/contracts/extension/Multicall.sol";
```

The `Multicall` smart contract is an extension usable with any base smart contract. It provides a function `multicall` to batch together multiple calls in a single external call.

<ViewContractCodeButton
  link="https://github.com/thirdweb-dev/contracts/blob/main/contracts/extension/Multicall.sol"
  name="Multicall"
/>

<br/>

:::info

It is recommended to use this extension alongside the [`ERC721Mintable`](/solidity/extensions/erc721mintable), [`ERC1155Mintable`](/solidity/extensions/erc1155mintable) or [`ERC20Mintable`](/solidity/extensions/erc20mintable)
extension to unlock the [`ERC721BatchMintable`](/solidity/extensions/erc721batchmintable), [`ERC1155BatchMintable`](/solidity/extensions/erc1155batchmintable) or [`ERC20BatchMintable`](/solidity/extensions/erc20batchmintable) dashboard extension detection.

:::

## Usage

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

import "@thirdweb-dev/contracts/extension/Multicall.sol";

contract MyContract is Multicall { ... }
```

## SDK Usage

This extension alone does not unlock anything extra in the SDKs.

## Dashboard Usage

This extension alone does not unlock anything in the Dashboard.

## Availability in base contracts

The `Multicall` extension is _already_ available in the following [base contracts](/solidity/base-contracts):

- All ERC20, ERC721 and ERC1155 base contracts.

## Full API reference

### multicall

```solidity
function multicall(bytes[] calldata data) external virtual override returns (bytes[] memory results);
```

- Receives and executes a batch of function calls on the contract.
- Parameter `data`: The bytes data that makes up the batch of function calls to execute.
- Return value `results`: The bytes data that makes up the result of the batch of function calls executed.
