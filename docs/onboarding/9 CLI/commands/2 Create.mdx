---
title: Create
slug: /cli/create
---

import CLI from "/docs/cli.json";
import CLIOptions from "@components/CLIOptions";

# Create

Create a new project with the thirdweb packages installed and configured.

Choose between creating apps with one of our [SDKs](/sdk),
[smart contracts](/solidity/base-contracts) or
[dynamic smart contract extensions](/solidity/extensions).

```bash
npx thirdweb create
```

## Usage

After running the command, you will be prompted to provide the:

1. Type of project to create (app, smart contract, or dynamic contract extension)
2. Name of your project

If you choose **App**, you will be prompted to provide the:

3. Blockchain evironment to use (EVM or Solana)
4. Framework to build with (Next.js, Create React App, Vite, React Native, Node.js or Express)
5. Language (JavaScript or TypeScript)

If you choose **Smart Contract** or **Dynamic Contract Extension** you will be prompted to provide the:

3. Framework to build with (Forge or HardHat)
4. Base Contract (choose from [ERC721](/solidity/extensions/base-contracts#erc721), [ERC1155](/solidity/extensions/base-contracts#erc1155) or [ERC20](/solidity/extensions/base-contracts#erc20), or empty)
5. Smart Contract name

```bash
npx thirdweb create [options] [projectPath]
```

## Options

<div>
  {CLI.create.map(({ flags, description }) => (
    <CLIOptions flags={flags} description={description} />
  ))}
</div>
