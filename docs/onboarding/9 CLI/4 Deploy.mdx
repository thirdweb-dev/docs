---
title: Deploy
slug: /cli/deploy
---

import CLI from "/docs/cli.json";
import CLIOptions from "@components/CLIOptions";

# Deploy

[Deploy](/deploy) your smart contracts to **Any EVM** compatible chain using the [dashboard](/dashboard). This command::

- Compiles your smart contracts
- Uploads the contract [ABI](https://docs.soliditylang.org/en/v0.8.17/abi-spec.html)s to IPFS
- Allows you to [deploy](/deploy) them through the dashboard

## Command

```bash
npx thirdweb deploy [options]
```

## Options

<div>
  {CLI.deploy.map(({ flags, description }) => (
    <CLIOptions flags={flags} description={description} />
  ))}
</div>
