---
title: ERC721Supply
slug: /extensions/erc721supply
---

# ERC721Supply

## `getAll`

Get all the data associated with every NFT in this contract.

By default, returns the first 100 NFTs, use `queryParams` to fetch more.

```javascript
const nfts = await contract.erc721.getAll();
console.log(nfts);
```
