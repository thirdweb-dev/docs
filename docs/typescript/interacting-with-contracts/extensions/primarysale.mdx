---
title: PrimarySale
slug: /sdk.contractprimarysale
toc_max_heading_level: 4 # Show up to h4 in table of contents (default is up to h3)
---

# PrimarySale

Functionality available for contracts that inherit the [`PrimarySale`](/contracts/PrimarySale) contract.

## getRecipient

Get the primary sale recipient.

```javascript
const salesRecipient = await contract.sales.getRecipient();
```

## setRecipient

Set the primary sale recipient

```javascript
await contract.sales.setRecipient(recipientWalletAddress);
```
