---
slug: /sdk/ui-components/connectwalletbutton
title: Connect Wallet Button
---

import QuickstartCard from "../../../../../../src/components/QuickstartCard";

# Connect Wallet Button

The Connect Wallet button allows the user to connect to your application using their
[MetaMask](/react/react.usemetamask),
[WalletConnect](/react/react.usewalletconnect) or [Coinbase](/react/react.usecoinbasewallet) wallet.

After the user has connected, the user can:

- View the connected wallet address
- View their balance of the **native token** _(such as ETH)_
- Switch network, disconnect wallet, and copy their wallet address

Below is an example of the button before and after the user connects their wallet:

<div
  style={{
    display: "flex",
    justifyContent: "center",
    marginBottom: 24,
  }}
>
  <img src={"/assets/connectwalletbutton.png"} style={{ maxHeight: 300 }} />
</div>

<div className="row" style={{ marginBottom: 24 }}>
  <div className="col col--12" style={{ marginTop: 8 }}>
    <QuickstartCard
      name="Example: Connect Wallet Button"
      link="https://connectwallet.thirdweb-example.com/"
      image="/assets/icons/education.png"
    />
  </div>
</div>

## Example

```jsx
import { ConnectWallet } from "@thirdweb-dev/react";

<ConnectWallet
  // Some customization of the button style
  colorMode="light"
  accentColor="#F213A4"
/>;
```
